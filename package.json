{
  "name": "myhabbit",
  "version": "11.1.2",
  "private": true,
  "description": "myHabbit family habit PWA on Cloudflare Workers",
  "scripts": {
    "check": "node --check src/worker.js",
    "validate": "node scripts/validate-project.mjs",
    "predeploy": "npm run check && npm run validate",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.116.0"
  },
  "engines": {
    "node": ">=20"
  }
}
